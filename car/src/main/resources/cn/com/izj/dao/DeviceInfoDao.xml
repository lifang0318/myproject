<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.izj.dao.DeviceInfoDao">

    <select id="getDeviceInfo" resultType="cn.com.izj.base.entity.DeviceInfo">
        select *
        from device_info
        where device_id = #{deviceId}
    </select>

    <update id="updateDeviceState">
        update device_info
        set state = #{state}, update_time = now()
        where device_id = #{deviceId}

    </update>

</mapper>